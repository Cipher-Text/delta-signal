# API

API design, endpoint documentation, and contract notes.

Start with the [backend route catalog](backend-api-links.md) for the current API surface. The [initial API](initial-api.md) is retained as a historical baseline proposal. Route contracts should be added to `packages/contracts` before frontend integration.

Backend route catalog: [backend-api-links.md](backend-api-links.md).
